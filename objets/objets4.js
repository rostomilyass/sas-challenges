let point ={
    x : 5,
    y: 7,
}
console.log(`les proprietes de ce point sont (${point.x};${point.y})`)
point.x=7
point.y=8
console.log(`les nouveaux proprietes de ce point sont (${point.x};${point.y})`)